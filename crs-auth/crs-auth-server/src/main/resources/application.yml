spring:
  application:
      name: CrsAuthServer

logging:
  file: ${HOME}/logs/${spring.application.name}.log
  level:
    root: info
  datasource:
    url: jdbc:h2:mem:test
    driver-class-name: org.h2.Driver
    username: root
    password: 123456